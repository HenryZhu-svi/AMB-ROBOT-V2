[
  {
    "id": "v2_tab_ui_adapter",
    "type": "tab",
    "label": "AMB-ROBOT-V2 UI Adapter",
    "disabled": false,
    "info": "uibuilder adapter for AMB-ROBOT-V2. Fleet Management remains unchanged."
  },
  {
    "id": "v2_seer_config",
    "type": "seer-config",
    "name": "AMB-20 Gateway",
    "host": "192.168.1.100",
    "port": "8090",
    "robotId": "AMB-20",
    "tenantId": "default",
    "timeout": 8000
  },
  {
    "id": "v2_uibuilder",
    "type": "uibuilder",
    "z": "v2_tab_ui_adapter",
    "name": "AMB-ROBOT-V2",
    "url": "AMB-ROBOT-V2",
    "fwdInMessages": false,
    "allowScripts": false,
    "allowStyles": false,
    "copyIndex": true,
    "sourceFolder": "src",
    "x": 180,
    "y": 120,
    "wires": [["v2_command_router"], ["v2_client_event"]]
  },
  {
    "id": "v2_command_router",
    "type": "function",
    "z": "v2_tab_ui_adapter",
    "name": "Validate, deduplicate and route UI commands",
    "func": "const topic = String(msg.topic || '');\nconst payload = msg.payload && typeof msg.payload === 'object' ? msg.payload : {};\nconst action = String(payload.action || '').trim().toLowerCase();\nconst commandId = String(payload.command_id || payload.request_id || '').trim();\nconst outputs = new Array(9).fill(null);\n\nif (topic === 'ui/state/request' || (topic === 'fleet/command/request' && action === 'sync')) {\n    msg.topic = 'ui/state/request';\n    outputs[0] = msg;\n    return outputs;\n}\nif (!commandId) {\n    outputs[0] = { topic: 'v2/command/rejected', payload: { error: 'command ID is required', original_topic: topic } };\n    return outputs;\n}\nlet processed = flow.get('v2_processed_commands') || [];\nif (!Array.isArray(processed)) processed = [];\nif (processed.includes(commandId)) {\n    outputs[0] = { topic: 'v2/command/duplicate', payload: { command_id: commandId } };\n    return outputs;\n}\nconst accepted = { topic: 'v2/command/accepted', payload: { command_id: commandId, action: action, params: payload.params || {}, accepted_at: new Date().toISOString() } };\nfunction markProcessed() {\n    processed.push(commandId);\n    if (processed.length > 200) processed = processed.slice(-200);\n    flow.set('v2_processed_commands', processed);\n    try { flow.set('v2_processed_commands', processed, 'file'); } catch (e) {}\n    outputs[0] = accepted;\n}\nif (topic === 'fleet/command/request' && action === 'start') { markProcessed(); outputs[1] = msg; return outputs; }\nif (topic === 'fleet/command/request' && action === 'cancel') { markProcessed(); outputs[2] = msg; return outputs; }\nif (topic !== 'edge/command/request') { outputs[8] = { topic: 'v2/command/rejected', payload: { command_id: commandId, error: 'unsupported topic or action' } }; return outputs; }\nif (action === 'navigate') {\n    const params = payload.params || {};\n    const target = String(params.target || params.destination || params.point || '').trim();\n    if (!target) outputs[8] = { topic: 'v2/command/rejected', payload: { command_id: commandId, error: 'navigation target is required' } };\n    else { markProcessed(); msg.payload = target; msg.v2Command = accepted.payload; outputs[3] = msg; }\n} else if (action === 'pause') { markProcessed(); outputs[4] = msg; }\nelse if (action === 'resume') { markProcessed(); outputs[5] = msg; }\nelse if (action === 'cancel') { markProcessed(); outputs[6] = msg; }\nelse if (action === 'settings/save') { markProcessed(); msg.topic = 'v2/config/save'; outputs[7] = msg; }\nelse outputs[8] = { topic: 'v2/command/rejected', payload: { command_id: commandId, error: 'unsupported action: ' + action } };\nreturn outputs;",
    "outputs": 9,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 470,
    "y": 120,
    "wires": [
      ["v2_state_manager"],
      ["v2_fleet_accept_out"],
      ["v2_fleet_cancel_out"],
      ["v2_robot_nav"],
      ["v2_robot_pause"],
      ["v2_robot_resume"],
      ["v2_robot_cancel"],
      ["v2_state_manager"],
      ["v2_state_manager"]
    ]
  },
  {
    "id": "v2_client_event",
    "type": "function",
    "z": "v2_tab_ui_adapter",
    "name": "uibuilder client event",
    "func": "msg.topic = 'v2/client/event';\nmsg.payload = { connected: true, socket_id: msg._socketId || null, event: msg.uibuilderCtrl || msg.payload || null, at: new Date().toISOString() };\nreturn msg;",
    "outputs": 1,
    "x": 450,
    "y": 180,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_state_manager",
    "type": "function",
    "z": "v2_tab_ui_adapter",
    "name": "Persistent UI state and snapshot reducer",
    "func": "const KEY = 'v2_runtime_state';\nfunction defaults() {\n    return { revision: 0, updatedAt: null, connection: { uibuilder: 'online', fleet: 'unknown' }, robot: { id: 'AMB-20', name: 'AMB-20', mode: 'unknown', battery: null, currentPoi: '-' }, navigation: { state: 'idle', source: null, from: null, destination: null }, task: null, config: { standbyPoint: 'HR775', chargingPoint: 'CP1', visiblePoints: [], fleetTasks: [] } };\n}\nfunction load() {\n    let value = null;\n    try { value = flow.get(KEY, 'file'); } catch (e) {}\n    return value || flow.get(KEY) || defaults();\n}\nfunction save(value) {\n    flow.set(KEY, value);\n    try { flow.set(KEY, value, 'file'); } catch (e) {}\n}\nfunction bump(value) { value.revision = Number(value.revision || 0) + 1; value.updatedAt = new Date().toISOString(); }\nfunction snapshot(value, request) { return { topic: 'ui/state/snapshot', payload: value, _socketId: request && request._socketId }; }\n\nconst state = load();\nconst topic = String(msg.topic || '');\nconst payload = msg.payload && typeof msg.payload === 'object' ? msg.payload : {};\nconst out = [];\nlet changed = false;\nlet forceSnapshot = false;\n\nif (topic === 'ui/state/request' || topic === 'v2/bootstrap' || topic === 'v2/client/event') {\n    state.connection.uibuilder = 'online';\n    forceSnapshot = true;\n} else if (topic === 'v2/robot/battery') {\n    const value = Number(payload.battery_pct ?? payload.battery ?? msg.payload);\n    if (Number.isFinite(value)) { state.robot.battery = value; changed = true; }\n} else if (topic === 'v2/robot/poi') {\n    state.robot.currentPoi = String(payload.current_poi || payload.poi || payload.point || msg.payload || '-');\n    changed = true;\n} else if (topic === 'v2/robot/mode') {\n    state.robot.mode = String(payload.mode || payload.status || msg.payload || 'unknown');\n    changed = true;\n} else if (topic === 'nav/started' || topic === 'nav/progress') {\n    state.navigation.state = 'running';\n    state.navigation.destination = payload.target || payload.destination || payload.poi || state.navigation.destination;\n    state.robot.mode = 'Moving';\n    changed = true;\n} else if (topic === 'nav/arrived') {\n    state.robot.currentPoi = String(payload.target || payload.destination || payload.poi || state.navigation.destination || state.robot.currentPoi);\n    state.robot.mode = 'Idle';\n    state.navigation = { state: 'idle', source: null, from: null, destination: null };\n    if (state.task && state.task.source === 'local') state.task = null;\n    changed = true;\n} else if (topic === 'nav/error') {\n    state.navigation.state = 'failed';\n    state.robot.mode = 'Error';\n    changed = true;\n} else if (topic === 'v2/config/save') {\n    const params = payload.params || {};\n    const section = String(params.section || '');\n    state.config[section] = params.values || {};\n    changed = true;\n    out.push({ topic: 'config/saved', payload: { request_id: payload.request_id, section: section, config: state.config } });\n} else if (topic === 'fleet/job-progress') {\n    const job = payload.job || {};\n    const step = payload.currentSubjob || payload.current_subjob || {};\n    state.connection.fleet = 'online';\n    state.task = payload.active === false ? null : { id: job.id || null, source: 'fleet', name: job.name || 'Fleet Task', state: 'running', currentStep: step.name || null, destination: step.destination || step.poi || null, progress: Number(payload.progress || 0) };\n    changed = true;\n} else if (topic === 'v2/command/accepted') {\n    const action = String(payload.action || '');\n    const params = payload.params || {};\n    if (action === 'navigate') { state.navigation = { state: 'submitting', source: params.source || 'local', from: state.robot.currentPoi, destination: params.target || params.destination || params.point || null }; }\n    if (action === 'pause') state.navigation.state = 'pausing';\n    if (action === 'resume') state.navigation.state = 'resuming';\n    if (action === 'cancel') state.navigation.state = 'cancelling';\n    if (action === 'start') state.connection.fleet = 'syncing';\n    changed = true;\n    out.push({ topic: 'ui/command/ack', payload: { request_id: payload.command_id, command_id: payload.command_id, action: action, accepted: true, revision: Number(state.revision || 0) + 1 } });\n} else if (topic === 'v2/command/duplicate') {\n    out.push({ topic: 'ui/command/ack', payload: { request_id: payload.command_id, command_id: payload.command_id, accepted: true, duplicate: true, revision: state.revision } });\n} else if (topic === 'v2/command/rejected' || topic === 'v2/error') {\n    out.push({ topic: 'ui/command/ack', payload: { request_id: payload.command_id || payload.request_id || null, command_id: payload.command_id || null, accepted: false, error: payload.error || 'command failed', revision: state.revision } });\n}\n\nconst globalStatus = String(global.get('current_status') || '').trim();\nconst globalJob = String(global.get('current_job_name') || '').trim();\nif (globalJob) {\n    state.connection.fleet = 'online';\n    state.task = Object.assign({}, state.task || {}, { source: 'fleet', name: globalJob, state: globalStatus && globalStatus !== 'idle' ? 'running' : 'available', currentStep: globalStatus || null });\n    changed = true;\n} else if (globalStatus === 'idle' && state.task && state.task.source === 'fleet') {\n    state.task = null;\n    changed = true;\n}\n\nif (changed) { bump(state); save(state); }\nif (changed || forceSnapshot) out.push(snapshot(state, msg));\nreturn [out];",
    "outputs": 1,
    "noerr": 0,
    "initialize": "",
    "finalize": "",
    "libs": [],
    "x": 1130,
    "y": 180,
    "wires": [["v2_uibuilder"]]
  },
  {
    "id": "v2_bootstrap",
    "type": "inject",
    "z": "v2_tab_ui_adapter",
    "name": "Restore saved state on deploy",
    "props": [{"p": "topic", "vt": "str"}],
    "repeat": "",
    "crontab": "",
    "once": true,
    "onceDelay": 0.5,
    "topic": "v2/bootstrap",
    "x": 210,
    "y": 260,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_poll",
    "type": "inject",
    "z": "v2_tab_ui_adapter",
    "name": "Poll robot state every 2s",
    "props": [{"p": "payload"}, {"p": "topic", "vt": "str"}],
    "repeat": "2",
    "once": true,
    "onceDelay": 1,
    "topic": "v2/poll",
    "payload": "",
    "payloadType": "date",
    "x": 190,
    "y": 380,
    "wires": [["v2_robot_battery", "v2_robot_poi", "v2_robot_mode"]]
  },
  {
    "id": "v2_robot_battery",
    "type": "robot-battery",
    "z": "v2_tab_ui_adapter",
    "name": "Battery",
    "server": "v2_seer_config",
    "pollInterval": "0",
    "x": 430,
    "y": 340,
    "wires": [["v2_battery_topic"], ["v2_error_normalizer"]]
  },
  {
    "id": "v2_robot_poi",
    "type": "robot-currentpoi",
    "z": "v2_tab_ui_adapter",
    "name": "Current POI",
    "server": "v2_seer_config",
    "pollInterval": "0",
    "x": 440,
    "y": 380,
    "wires": [["v2_poi_topic"], ["v2_error_normalizer"]]
  },
  {
    "id": "v2_robot_mode",
    "type": "robot-mode",
    "z": "v2_tab_ui_adapter",
    "name": "Robot Mode",
    "server": "v2_seer_config",
    "pollInterval": "0",
    "x": 440,
    "y": 420,
    "wires": [["v2_mode_topic"], ["v2_error_normalizer"]]
  },
  {
    "id": "v2_battery_topic",
    "type": "change",
    "z": "v2_tab_ui_adapter",
    "name": "battery → state",
    "rules": [{"t": "set", "p": "topic", "pt": "msg", "to": "v2/robot/battery", "tot": "str"}],
    "x": 680,
    "y": 340,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_poi_topic",
    "type": "change",
    "z": "v2_tab_ui_adapter",
    "name": "POI → state",
    "rules": [{"t": "set", "p": "topic", "pt": "msg", "to": "v2/robot/poi", "tot": "str"}],
    "x": 670,
    "y": 380,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_mode_topic",
    "type": "change",
    "z": "v2_tab_ui_adapter",
    "name": "mode → state",
    "rules": [{"t": "set", "p": "topic", "pt": "msg", "to": "v2/robot/mode", "tot": "str"}],
    "x": 680,
    "y": 420,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_robot_nav",
    "type": "robot-nav",
    "z": "v2_tab_ui_adapter",
    "name": "Navigate",
    "server": "v2_seer_config",
    "stationId": "",
    "sourceId": "SELF_POSITION",
    "maxSpeed": "0.7",
    "maxWspeed": "0.5",
    "direction": "0",
    "x": 820,
    "y": 520,
    "wires": [["v2_nav_arrived"], ["v2_nav_error"], ["v2_state_manager"]]
  },
  {
    "id": "v2_robot_pause",
    "type": "robot-pause",
    "z": "v2_tab_ui_adapter",
    "name": "Pause",
    "server": "v2_seer_config",
    "x": 810,
    "y": 560,
    "wires": [[], ["v2_error_normalizer"]]
  },
  {
    "id": "v2_robot_resume",
    "type": "robot-resume",
    "z": "v2_tab_ui_adapter",
    "name": "Resume",
    "server": "v2_seer_config",
    "x": 810,
    "y": 600,
    "wires": [[], ["v2_error_normalizer"]]
  },
  {
    "id": "v2_robot_cancel",
    "type": "robot-cancel",
    "z": "v2_tab_ui_adapter",
    "name": "Cancel Local Navigation",
    "server": "v2_seer_config",
    "x": 850,
    "y": 640,
    "wires": [[], ["v2_error_normalizer"]]
  },
  {
    "id": "v2_nav_arrived",
    "type": "change",
    "z": "v2_tab_ui_adapter",
    "name": "navigation arrived",
    "rules": [{"t": "set", "p": "topic", "pt": "msg", "to": "nav/arrived", "tot": "str"}],
    "x": 1080,
    "y": 520,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_nav_error",
    "type": "change",
    "z": "v2_tab_ui_adapter",
    "name": "navigation error",
    "rules": [{"t": "set", "p": "topic", "pt": "msg", "to": "nav/error", "tot": "str"}],
    "x": 1070,
    "y": 560,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_error_normalizer",
    "type": "function",
    "z": "v2_tab_ui_adapter",
    "name": "Normalize command error",
    "func": "const original = msg.payload;\nmsg.topic = 'v2/error';\nmsg.payload = { command_id: msg.v2Command && msg.v2Command.command_id || null, error: original && (original.error || original.message) || msg.error && msg.error.message || 'gateway command failed', detail: original || null };\nreturn msg;",
    "outputs": 1,
    "x": 1080,
    "y": 640,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_fleet_accept_out",
    "type": "link out",
    "z": "v2_tab_ui_adapter",
    "name": "Start → existing Fleet accept engine",
    "mode": "link",
    "links": ["c21761218db4c6d5"],
    "x": 835,
    "y": 240,
    "wires": []
  },
  {
    "id": "v2_fleet_cancel_out",
    "type": "link out",
    "z": "v2_tab_ui_adapter",
    "name": "Cancel → existing Fleet cancel engine",
    "mode": "link",
    "links": ["ee6ac5bac94adea3"],
    "x": 835,
    "y": 280,
    "wires": []
  },
  {
    "id": "v2_fleet_progress_in",
    "type": "link in",
    "z": "v2_tab_ui_adapter",
    "name": "Fleet progress input",
    "links": [],
    "x": 775,
    "y": 700,
    "wires": [["v2_fleet_progress_topic"]]
  },
  {
    "id": "v2_fleet_progress_topic",
    "type": "change",
    "z": "v2_tab_ui_adapter",
    "name": "Fleet progress → state",
    "rules": [{"t": "set", "p": "topic", "pt": "msg", "to": "fleet/job-progress", "tot": "str"}],
    "x": 1030,
    "y": 700,
    "wires": [["v2_state_manager"]]
  },
  {
    "id": "v2_status_debug",
    "type": "debug",
    "z": "v2_tab_ui_adapter",
    "name": "V2 adapter diagnostics",
    "active": false,
    "tosidebar": true,
    "console": false,
    "tostatus": false,
    "complete": "true",
    "targetType": "full",
    "x": 1330,
    "y": 720,
    "wires": []
  }
]
